# Mediasoup Express Server (minimal)
Instructions:
1. cd server
2. npm install
3. npm run dev   (or npm start)
This is a minimal demo server. You may need to adapt listen IPs and mediasoup worker options for your environment.
